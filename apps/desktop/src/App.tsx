import { Button } from '@ui-kit/components/button/Button';

const App = () => {
  return (
    <div style={{ backgroundColor: 'red' }}>
      <h1 style={{ color: '#ffffff' }}>Hello from Electron!</h1>
      <Button>Click me!</Button>
    </div>
  );
};

export default App;
